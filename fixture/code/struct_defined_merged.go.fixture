package model

// stride:generate
type User struct {
	// stride:generate
	ID string
	// stride:generate
	FirstName string
	// stride:generate
	LastName string
}

// stride:define
type ID string

// stride:define
type Users []*User

// stride:define
type Company struct {
	// stride:define:field name
	Name string
}

// stride:define
func (u *User) FullName() string {
	return u.FirstName + " " + u.LastName
}
